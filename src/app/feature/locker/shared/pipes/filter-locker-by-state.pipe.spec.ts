import { FilterLockerByStatePipe } from './filter-locker-by-state.pipe';

describe('FilterLockerByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLockerByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
